# Smoke config for local / CI — no KiCad dependency (issue #44).

spec_version: 1
spice_engine: ngspice

netlist: rc.cir

scenarios:
  - id: divider_op
    measures:
      - id: v_n2
        min: 4.99
        max: 5.01
