name: KiCad Design Checks

# Run on every PR so CI/shared changes (.github, kibot, scripts, etc.) are still validated.
# (A narrow "paths" filter was hiding the whole workflow when a PR only touched those dirs.)
on:
  pull_request:

jobs:
  detect-boards:
    runs-on: ubuntu-latest
    outputs:
      boards: ${{ steps.detect.outputs.boards }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect which boards to check
        id: detect
        run: |
          set -euo pipefail
          BASE="origin/${{ github.base_ref }}"
          files=$(git diff --name-only "$BASE...HEAD" || true)

          all_boards() {
            # Do not let the test `ls` print to stdout — it was being captured by
            # CHANGED=$(all_boards) and duplicated every board (e.g. boards/foo/ + foo).
            if ls -d boards/*/ >/dev/null 2>&1; then
              ls -d boards/*/ 2>/dev/null | xargs -n1 basename | sort -u
            else
              echo ""
            fi
          }

          # Shared or fab paths → validate every board (CI, libs, or rules can affect all designs)
          if echo "$files" | grep -qE '^(libs/|fab-rules/|\.github/|kibot/|scripts/)' || echo "$files" | grep -qE '^Makefile$'; then
            CHANGED=$(all_boards)
          # Touch under boards/<name>/ → check those project(s) only
          elif echo "$files" | grep -qE '^boards/'; then
            CHANGED=$(echo "$files" | grep -E '^boards/[^/]+/' | cut -d/ -f2 | sort -u)
          # e.g. README, docs, license only — skip heavy KiCad runs
          else
            CHANGED=""
          fi

          if [ -z "$CHANGED" ]; then
            echo "boards=[]" >> "$GITHUB_OUTPUT"
            echo "No hardware or shared-design paths in this diff — KiCad jobs skipped (see workflow comment)."
          else
            BOARDS=$(echo "$CHANGED" | jq -R -s -c 'split("\n") | map(select(. != ""))')
            echo "boards=$BOARDS" >> "$GITHUB_OUTPUT"
            echo "Boards to check: $BOARDS"
          fi

      - name: Report detected boards
        run: echo "Boards to check:\ ${{ steps.detect.outputs.boards }}"

  erc:
    needs: detect-boards
    if: needs.detect-boards.outputs.boards != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        board: ${{ fromJson(needs.detect-boards.outputs.boards) }}
    steps:
      - uses: actions/checkout@v4

      - name: Run ERC
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace \
            kicad/kicad:10.0 \
            kicad-cli sch erc \
              --exit-code-violations \
              --format json \
              --output "boards/${{ matrix.board }}/erc.json" \
              "boards/${{ matrix.board }}/${{ matrix.board }}.kicad_sch"

      - name: Upload ERC report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: erc-${{ matrix.board }}
          path: boards/${{ matrix.board }}/erc.json

  drc:
    needs: detect-boards
    if: needs.detect-boards.outputs.boards != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        board: ${{ fromJson(needs.detect-boards.outputs.boards) }}
    steps:
      - uses: actions/checkout@v4

      - name: Run DRC (default rules, schematic parity)
        # kicad-actions does not pass --schematic-parity; use kicad-cli in official KiCad 10 image
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace \
            kicad/kicad:10.0 \
            kicad-cli pcb drc \
              --exit-code-violations \
              --refill-zones \
              --schematic-parity \
              --format json \
              --output "boards/${{ matrix.board }}/drc-default.json" \
              "boards/${{ matrix.board }}/${{ matrix.board }}.kicad_pcb"

      - name: Upload DRC report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: drc-default-${{ matrix.board }}
          path: boards/${{ matrix.board }}/drc-default.json

  fab-drc:
    needs: detect-boards
    if: needs.detect-boards.outputs.boards != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        board: ${{ fromJson(needs.detect-boards.outputs.boards) }}
    steps:
      - uses: actions/checkout@v4

      - name: Run multi-fab DRC
        # Same KiCad 10.x as DRC job (avoids mismatch with kicad-nightly)
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            -w /workspace \
            kicad/kicad:10.0 \
            bash scripts/run-drc-all-fabs.sh boards/${{ matrix.board }}

      - name: Upload fab DRC reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: drc-fabs-${{ matrix.board }}
          path: boards/${{ matrix.board }}/drc-*.json

  kicad-failure-report:
    name: Post KiCad failure summary on PR
    if: always() && github.event_name == 'pull_request' && !cancelled() && (needs.erc.result == 'failure' || needs.drc.result == 'failure' || needs.fab-drc.result == 'failure')
    needs: [erc, drc, fab-drc]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download KiCad report artifacts
        uses: actions/download-artifact@v4
        with:
          path: ci-artifacts

      - name: List downloaded artifacts
        if: always()
        run: ls -laR ci-artifacts 2>/dev/null || echo "ci-artifacts directory not found"

      - name: Compose PR comment
        env:
          GITHUB_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          ERC_RESULT: ${{ needs.erc.result }}
          DRC_RESULT: ${{ needs.drc.result }}
          FAB_DRC_RESULT: ${{ needs.fab-drc.result }}
        run: bash scripts/ci/compose-kicad-failure-comment.sh ci-artifacts | tee pr-comment.md

      - name: Post sticky PR comment
        uses: marocchino/sticky-pull-request-comment@v2.9.1
        with:
          header: kicad-design-checks
          path: pr-comment.md
